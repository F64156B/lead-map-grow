

# Adicionar Logos Adimax e DPA na Tela de Login

## O que sera feito

Copiar as duas imagens enviadas para o projeto e exibi-las na tela de login, substituindo o icone atual (clipboard roxo).

## Layout

- **Logo DPA** (Desenvolvimento Profissional Adimax) no topo do card, centralizada
- **Logo Adimax** menor, abaixo do titulo ou no rodape do card

## Arquivos afetados

1. **Copiar assets**: `Adimax.png` e `DPA.png` para `src/assets/`
2. **`src/pages/Login.tsx`**: Remover o icone `ClipboardCheck` e substituir pelas duas logos importadas via ES6 modules

